class Product {
  String image, nombre, date, time, descripcion;
  double precio;
  int stock;
  Product(this.image, this.nombre, this.precio, this.stock, this.date,
      this.time, this.descripcion);
}
